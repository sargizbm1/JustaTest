{
  "name": "commons",
  "version": "3.0.0",
  "private": true,
  "description": "A simple chat app with IDs, contacts and direct messages",
  "main": "server.js",
  "scripts": { "start": "node server.js" },
  "engines": { "node": ">=18" }
}
